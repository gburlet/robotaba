class PolyphonicTranscription(object):

    def __call__(self, audio_url, audio_path):
        '''
        Your polyphonic transcription algorithm should be placed here.
        Create an MEI file (with timeline linked to the given audio_url)
        of note event estimates from the audio file given by audio_path.
        Return the contents of the MEI file as a string. Robotaba will take
        care of writing the file in the proper place and entering the appropriate
        information in the database.

        PARAMATERS
        ----------
        audio_url (String): absolute url of the audio file on the server
        audio_path (String): path to the audio file on the server HDD
        '''

        return mei_str
